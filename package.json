{
	"name": "yellow-pond-67d9",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"seed": "node scripts/convert_xlsx_to_sql.js && wrangler d1 execute shop_db --local --file=./seed_products.sql"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.2.2",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.63.0",
		"xlsx": "^0.18.5"
	}
}
